import Layout from "../../components/Layout";
import { gql, useQuery } from '@apollo/client'
const Index = () => {
    return (
      <div>
        <Layout>
          
        </Layout>
      </div>
    );
}

export default Index
